{
  "name": "transactiongenerationapi",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node TranGenAPI.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "build": "npm run build:clean && npm run build:copy",
    "build:clean": "rm -rf dist ",
    "build:copy": "mkdir -p dist && cp *.js dist/ && cp package*.json dist/",
    "prebuild": "npm run test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parser": "^3.2.0",
    "dotenv": "^17.1.0",
    "express": "^5.1.0",
    "fs": "^0.0.1-security",
    "joi": "^17.13.3",
    "kafka-node": "^5.0.0",
    "kafkajs": "^2.2.4",
    "mongodb": "^6.17.0",
    "multer": "^2.0.1",
    "jest-junit": "^16.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-junit": "^16.0.0",
    "mongodb-memory-server": "^8.12.0",
    "supertest": "^6.3.0"
  }
}
