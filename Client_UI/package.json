{
  "name": "client_ui",
  "version": "1.0.0",
  "main": "script.js",
  "scripts": {
    "test": "echo \"No tests for UI\"",
    "build": "npm run build:assets && npm run build:dist",
    "build:assets": "cp -r assets dist/ ",
    "build:dist": "mkdir -p dist && cp *.js dist/ && cp *.html dist/",
    "start": "node User_UI.js",
    "dev": "nodemon User_UI.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "nodemon": "^3.1.10",
    "startbootstrap-grayscale": "^7.0.6",
    "jest-junit": "^16.0.0"
  }
}
